# PostgreSQL for Auth Service
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-memory-ss
  namespace: agentica-app
spec:
  serviceName: "postgresql-memory-svc" # Must match the service name
  replicas: 1
  selector:
    matchLabels:
      app: postgres-memory
  template:
    metadata:
      labels:
        app: postgres-memory
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine # Use a specific version
          env:
            - name: POSTGRES_DB
              value: "agentica_memory"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: POSTGRES_MEMORY_PASSWORD
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-memory-storage
              mountPath: /var/lib/postgresql/data
          resources: # Crucial for your 16GB RAM
            requests:
              memory: "64Mi"
              cpu: "200m"
            limits:
              memory: "124Mi"
              cpu: "400m"
  volumeClaimTemplates: # This links to the PVC
    - metadata:
        name: postgres-memory-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 200Mi
