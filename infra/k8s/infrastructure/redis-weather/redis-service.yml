# Service for Redis
apiVersion: v1
kind: Service
metadata:
  name: redis-weather-svc
  namespace: agentica-app
spec:
  selector:
    app: redis-weather
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  clusterIP: None # Headless service for StatefulSet