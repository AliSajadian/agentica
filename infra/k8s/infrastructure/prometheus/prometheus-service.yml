apiVersion: v1
kind: Service
metadata:
  name: prometheus-svc
  namespace: agentica-app
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
  clusterIP: None  # Headless service for StatefulSet
