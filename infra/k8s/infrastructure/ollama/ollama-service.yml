# Headless Service for Ollama StatefulSet
apiVersion: v1
kind: Service
metadata:
  name: ollama-svc
  namespace: agentica-app
spec:
  selector:
    app: ollama
  ports:
    - name: http
      protocol: TCP
      port: 11434
      targetPort: 11434
  clusterIP: None  # Headless service [citation:1]