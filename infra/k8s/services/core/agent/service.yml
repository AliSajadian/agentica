# Service for Memory Service (HTTP & gRPC)
apiVersion: v1
kind: Service
metadata:
  name: agent-service-svc
  namespace: agentica-app
spec:
  selector:
    app: agent-service
  ports:
    - name: http-api
      protocol: TCP
      port: 8004 # Internal service port for HTTP
      targetPort: 8004 # Container's HTTP port
    # - name: grpc-api
    #   protocol: TCP
    #   port: 50052
    #   targetPort: 50052
  type: ClusterIP